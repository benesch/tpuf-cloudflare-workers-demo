{
  "name": "turbopuffer-cloudflare-worker",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that queries Turbopuffer using Vite",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "vite build",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev --local"
  },
  "keywords": [
    "cloudflare-workers",
    "turbopuffer",
    "vite"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@turbopuffer/turbopuffer": "^0.10.5"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.9.0",
    "vite": "^7.0.2",
    "wrangler": "^4.23.0"
  }
}
